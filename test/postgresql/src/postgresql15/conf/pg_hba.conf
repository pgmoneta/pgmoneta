local   all             all                                       trust
local   replication     all                                       trust
host    PG_DATABASE     PG_USER_NAME         all                  trust
host    all             PG_USER_NAME         all                  trust
local   postgres        PG_REPL_USER_NAME                         scram-sha-256
host    postgres        PG_REPL_USER_NAME    all                  scram-sha-256
host    replication     PG_REPL_USER_NAME    all                  scram-sha-256