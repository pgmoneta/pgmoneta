[pgmoneta]
host = localhost
metrics = 5001

base_dir = /tmp/pgmoneta

compression = zstd

retention = 7

log_type = console
log_level = info
log_path =

unix_socket_dir = /tmp/

[pgmoneta-walbridge]
host = localhost
port = 9970

[primary]
host = localhost
port = 5432
user = repl
wal_slot = repl
